<h1>Eat-Da-Burger!</h1>

<ol>
  <div class="clearfix">
  {{#each burgers}} {{#unless devoured}} {{> burgersPartial devoured=true}} {{/unless}} {{/each}}
  </div>
</ol>

<ol>
  {{#each burgers}} {{#if devoured}} {{> burgersPartial devoured=false}} {{/if}} {{/each}}
</ol>

<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
    <button type="submit">Submit Burger</button>
  </div>

</form>

<h3>Enter a burger you want to eat and click the 'Submit' button.</h3>